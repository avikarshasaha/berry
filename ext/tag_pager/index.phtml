<?=(${prev} ? '<a href="'.$prev.'">←</a>' : '←'); ?>

<? foreach ($pages as $page => $href){ ?>
    <?=(${page} == $current ? '<b>['.$page.']</b>' : '<a href="'.$href.'">['.$page.']</a>'); ?>
<? } ?>

<?=(${next} ? '<a href="'.$next.'">→</a>' : '→'); ?>