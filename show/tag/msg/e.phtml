<fieldset class="msg e">
    <legend>Ошибка <a href="#" onclick="Element.hide(this.parentNode.parentNode); return false;" title="Спрятать">x</a></legend>
    <? foreach ($messages as $message){ ?>
        <li>${message}</li>
    <? } ?>
</fieldset>